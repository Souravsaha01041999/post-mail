<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/inbox"
            android:icon="@drawable/inbox_icon"
            android:iconTint="@color/orange"
            android:title="Receive" />
        <item
            android:id="@+id/outbox"
            android:icon="@drawable/outbox_icon"
            android:iconTint="@color/orange"
            android:title="Sended" />
        <item
            android:id="@+id/changepass"
            android:icon="@drawable/passcng_icon"
            android:iconTint="@color/orange"
            android:title="Change password"
            />
        <item
            android:id="@+id/appinfo"
            android:title="App info"
            android:iconTint="@color/orange"
            android:icon="@drawable/info_icon"
            />
        <item
            android:id="@+id/logout"
            android:icon="@drawable/logout_icon"
            android:iconTint="@color/orange"
            android:title="Logout" />
    </group>
</menu>
